{
  "name": "figma-mermaid",
  "version": "0.1.0",
  "description": "Convert Mermaid flowchart syntax into native FigJam shapes and connectors",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mermaid": "^11.0.0"
  }
}
